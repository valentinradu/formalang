use stdlib::*

pub struct Modal: View {
  isOpen: Boolean,
  title: String,
  mount body: View
}

impl Modal {
  isOpen: false,
  title: "test",
  body: if self.isOpen {
    Label(content: self.title, fill: fill::Solid(color: .hex(value: "#000000")))
  } else {
    Empty()
  }
}
