// expect: run
// docs/user/structs.md: a comma separates two fields, and a line break
// after the comma is optional.
pub struct Point {
  x: I32,
  y: I32
}

pub fn run_checks() {
    let p = Point(x: 1, y: 2)
    assert(condition: p.x + p.y == 3)
}
