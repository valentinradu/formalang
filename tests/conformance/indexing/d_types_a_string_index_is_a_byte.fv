// expect: run
// docs/user/types.md: `"abc"[0]` is an I32, the byte at that offset.
pub fn run_checks() {
    let c: I32 = "abc"[0]
    assert(condition: c == 97)
    assert(condition: "abc"[2] == 99)
}
