// expect: run
// A struct of plain fields compares field by field.

pub struct Point {
    x: I32,
    y: I32
}

pub fn run_checks() {
    assert(condition: Point(x: 1, y: 2) == Point(x: 1, y: 2))
    assert(condition: Point(x: 1, y: 2) != Point(x: 1, y: 3))
}
