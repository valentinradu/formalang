// expect: run
// docs/user/expressions.md: equality compares each field of a struct.
pub struct P { x: I32, y: I32 }

pub fn run_checks() {
    assert(condition: P(x: 1, y: 2) == P(x: 1, y: 2))
    assert(condition: P(x: 1, y: 2) != P(x: 1, y: 3))
}
