// expect: run
pub fn run_checks() {
    let xs: [I32] = [2, 4, 6]
    assert(condition: for x in xs { x }.all(f: (v) -> v % 2 == 0))
    assert(condition: for x in xs { x }.any(f: (v) -> v == 4))
    assert(condition: !for x in xs { x }.any(f: (v) -> v == 5))
}
