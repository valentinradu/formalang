// expect: run
pub fn run_checks() {
    let xs: [I32] = [1, 2, 3, 4]
    assert(condition: for x in xs { x }.fold(initial: 0, f: (a, b) -> a + b) == 10)
    assert(condition: for x in xs { x }.count() == 4)
}
