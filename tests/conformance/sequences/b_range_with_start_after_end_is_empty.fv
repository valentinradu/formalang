// expect: run
// docs/user/expressions.md: `..` goes from the start (inclusive) to the
// end (exclusive) in steps of one. A start after the end gives nothing.
pub fn run_checks() {
    assert(condition: for i in 5..1 { i }.count() == 0)
    assert(condition: for i in 3..3 { i }.count() == 0)
}
