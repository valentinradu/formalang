// expect: run
// docs/user/types.md: `let mut b: [I32?] = [3]` is ok, because a nil
// may be put there later.
pub fn run_checks() {
    let mut b: [I32?] = [3]
    assert(condition: b.len() == 1)
    b = [nil]
    assert(condition: b.len() == 1)
}
