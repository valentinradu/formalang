// expect: run
pub fn run_checks() {
    let a: I32? = nil
    assert(condition: a.is_none())
    assert(condition: !a.is_some())
    let b: I32? = 1
    assert(condition: b.is_some())
    assert(condition: !b.is_none())
}
