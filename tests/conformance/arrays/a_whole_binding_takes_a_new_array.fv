// expect: run
// The other half of the rule: a `let mut` binding takes a new array.
// Only the elements of the old one are immutable.
pub fn run_checks() {
    let mut xs: [I32] = [1, 2]
    xs = [3, 4, 5]
    assert(condition: xs.len() == 3)
}
