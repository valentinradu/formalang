// expect: run
// docs/user/expressions.md: `...` can appear anywhere in array
// destructuring.
pub fn run_checks() {
    let items = [1, 2, 3, 4]
    let [a, ...mid, z] = items
    assert(condition: a == 1)
    assert(condition: z == 4)
    assert(condition: mid.len() == 2)
}
