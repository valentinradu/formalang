// expect: run
// docs/user/expressions.md: `...` can appear anywhere in array
// destructuring.
pub fn run_checks() {
    let items = [1, 2, 3, 4]
    let [...front, last] = items
    assert(condition: last == 4)
    assert(condition: front.len() == 3)
}
