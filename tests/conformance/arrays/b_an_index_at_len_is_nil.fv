// expect: run
// docs/user/expressions.md: an index at the length is out of range.
pub fn run_checks() {
    let xs: [I32] = [1, 2, 3]
    assert(condition: xs[3] == nil)
    assert(condition: xs[2] != nil)
}
