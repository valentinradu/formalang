// expect: run
pub fn run_checks() {
    let xs: [I32] = [1, 2, 3]
    assert(condition: xs.len() == 3)
    assert(condition: !xs.is_empty())
    let empty: [I32] = []
    assert(condition: empty.is_empty())
    assert(condition: empty.len() == 0)
}
