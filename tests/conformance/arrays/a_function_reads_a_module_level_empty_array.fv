// expect: run
// The type of `let EMPTY = []` is settled only when its value lowers.
// A function that reads it lowers first, and still gets its type.
let EMPTY = []

fn size() -> I32 {
    let e = EMPTY
    e.len()
}

pub fn run_checks() {
    assert(condition: size() == 0)
}
