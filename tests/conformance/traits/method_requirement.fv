// expect: run
pub trait Shape {
    fn area(self) -> I32
}

pub struct Square {
    side: I32
}

impl Shape for Square {
    fn area(self) -> I32 {
        self.side * self.side
    }
}

pub fn run_checks() {
    assert(condition: Square(side: 5).area() == 25)
}
