// expect: run
// docs/user/expressions.md: `pub let [_, second, ...] = items`.
pub let items = ["first", "second", "third", "fourth"]
pub let [_, second, ...] = items

pub fn run_checks() {
    assert(condition: second == "second")
}
