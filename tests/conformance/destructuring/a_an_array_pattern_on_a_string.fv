// expect: reject ArrayDestructuringNotArray
// from: rust tests/ui/pattern/pat-tuple-bad-type.rs and
// struct-pattern-on-non-struct-resolve-error.rs. An array pattern does not fit
// a string. This must be an error the user can act on, not an
// internal error.

pub struct P {
    a: I32,
    b: I32
}

pub fn f() -> I32 {
    let v: String = "ab"
    let [a, b] = v
    0
}
