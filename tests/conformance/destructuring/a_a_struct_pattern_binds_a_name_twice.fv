// expect: reject DuplicateDefinition
// from: rust tests/ui/pattern/ (E0416). One pattern binds `a` twice.
pub struct P { x: I32, y: I32 }
pub fn f(p: P) -> I32 {
    let {x as a, y as a} = p
    a
}
