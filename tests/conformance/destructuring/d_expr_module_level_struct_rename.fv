// expect: run
// docs/user/expressions.md: `pub let {name as username} = user`.
pub struct User { name: String, age: I32 }
pub let user = User(name: "Alice", age: 30)
pub let {name as username} = user

pub fn run_checks() {
    assert(condition: username == "Alice")
}
