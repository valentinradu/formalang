// expect: reject TypeMismatch
// from: rust tests/ui/pattern/pat-tuple-bad-type.rs and
// struct-pattern-on-non-struct-resolve-error.rs. A tuple pattern does not fit
// a range. This must be an error the user can act on, not an
// internal error.

pub struct P {
    a: I32,
    b: I32
}

pub fn f() -> I32 {
    let v: Range<I32> = 0..2
    let (a, b) = v
    0
}
