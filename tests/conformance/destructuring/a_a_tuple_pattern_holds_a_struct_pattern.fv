// expect: run
// A tuple pattern can hold a struct pattern.
pub struct P { x: I32, y: I32 }
pub fn f(t: (p: P, n: I32)) -> I32 {
    let ({x, y}, n) = t
    x + y + n
}
pub fn run_checks() {
    assert(condition: f(t: (p: P(x: 1, y: 2), n: 3)) == 6)
}
