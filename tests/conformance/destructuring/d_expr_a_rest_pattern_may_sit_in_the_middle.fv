// expect: run
// docs/user/expressions.md: `...` can appear anywhere in array
// destructuring.
pub fn run_checks() {
    let [first, ..., last] = [1, 2, 3, 4]
    assert(condition: first == 1)
    assert(condition: last == 4)
}
