// expect: run
// docs/user/expressions.md: struct destructuring is by field name, not by
// position.
pub struct P { a: I32, b: I32 }

pub fn run_checks() {
    let {b, a} = P(a: 1, b: 2)
    assert(condition: a == 1)
    assert(condition: b == 2)
}
