// expect: run
// docs/user/expressions.md: a string in triple quotes spans lines.
pub fn run_checks() {
    let s = """
  Multi-line
  string literal
"""
    assert(condition: s.contains(needle: "Multi-line"))
    assert(condition: s.contains(needle: "string literal"))
}
