// expect: run
// docs/user/types.md: keys can be a struct.
pub struct P { x: I32 }

pub fn run_checks() {
    let d: [P: I32] = [P(x: 1): 10, P(x: 2): 20]
    let v: I32 = if let n = d[P(x: 2)] { n } else { 0 }
    assert(condition: v == 20)
}
