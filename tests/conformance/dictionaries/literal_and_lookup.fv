// expect: run
pub fn run_checks() {
    let d: [String: I32] = ["a": 1, "b": 2]
    assert(condition: d.len() == 2)
    assert(condition: !d.is_empty())
    assert(condition: d["a"].is_some())
    assert(condition: d["zz"].is_none())
}
