// expect: run
// docs/user/types.md: a repeated key makes one entry, with the later value.
pub fn run_checks() {
    let d: [String: I32] = ["a": 1, "b": 2, "a": 3]
    assert(condition: d.len() == 2)
    if let v = d["a"] { assert(condition: v == 3) } else { assert(condition: false) }
}
