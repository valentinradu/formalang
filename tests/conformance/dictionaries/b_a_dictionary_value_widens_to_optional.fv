// expect: run
// docs/user/types.md: `[T]` fits `[T?]`, and the same holds for a
// dictionary's values.
fn f(d: [String: I32?]) -> I32 { d.len() }

pub fn run_checks() {
    let d: [String: I32] = ["a": 1]
    assert(condition: f(d: d) == 1)
}
