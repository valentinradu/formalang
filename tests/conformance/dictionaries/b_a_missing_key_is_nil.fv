// expect: run
// docs/user/expressions.md: the key may be absent, and the lookup gives
// nil.
pub fn run_checks() {
    let d: [String: I32] = ["a": 1]
    assert(condition: d["b"] == nil)
    assert(condition: d[""] == nil)
}
