// expect: run
// docs/user/types.md: keys can be a Boolean.
pub fn run_checks() {
    let d: [Boolean: String] = [true: "yes", false: "no"]
    let v: String = if let s = d[false] { s } else { "" }
    assert(condition: v == "no")
}
