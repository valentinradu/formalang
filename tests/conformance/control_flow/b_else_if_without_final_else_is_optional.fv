// expect: reject FunctionReturnTypeMismatch
// docs/user/control-flow.md: an `if` with no `else` gives an optional.
// An `else if` chain with no final `else` has no `else` at the end.
pub fn f(x: I32) -> I32 {
    if x > 1 { 1 } else if x > 0 { 2 }
}
