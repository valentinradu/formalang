// expect: run
// docs/user/control-flow.md: an `if` with no `else` returns nil if false.
fn pick(c: Boolean) -> I32? { if c { 1 } }

pub fn run_checks() {
    assert(condition: pick(c: false) == nil)
    let v: I32 = if let n = pick(c: true) { n } else { 0 }
    assert(condition: v == 1)
}
