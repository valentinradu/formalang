// expect: reject ParseError
// docs/user/core.md: there is no `while` in the language, so `while` is
// a name and the rest of its line is a second statement on the same
// line.
pub fn f() -> I32 {
    while true { 1 }
    0
}
