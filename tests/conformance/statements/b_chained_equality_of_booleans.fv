// expect: run
// docs/user/expressions.md: `==` is left-associative over values of one
// type. `true == false == false` is `(true == false) == false`.
pub fn run_checks() {
    assert(condition: true == false == false)
}
