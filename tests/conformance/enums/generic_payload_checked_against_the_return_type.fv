// expect: reject TypeMismatch
// And in return position. The return type gives `T` the type `I32`,
// so the payload is the error, as for a non-generic enum.

pub enum Maybe<T> {
    some(v: T),
    none
}

pub fn f() -> Maybe<I32> {
    Maybe.some(v: "text")
}
