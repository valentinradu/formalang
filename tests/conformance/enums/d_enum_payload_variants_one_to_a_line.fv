// expect: reject ParseError
// docs/user/enums.md: a comma separates two variants. A line break
// alone does not, for variants with data too. PLAN.md phase 2, item 1
// (FD4).
pub enum Message {
  text(content: String)
  image(url: String, size: I32)
}
