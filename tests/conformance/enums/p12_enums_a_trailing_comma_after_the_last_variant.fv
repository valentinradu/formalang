// expect: run
// docs/user/enums.md: a comma separates two variants, and a comma after
// the last variant is allowed.
pub enum Status {
    pending,
    active,
}

pub fn run_checks() {
    let s: Status = .active
    assert(condition: s == Status.active)
}
