// expect: reject TypeMismatch
// A qualified call finds the function in its module, so its arguments
// are checked. It found no function before, and checked nothing.

pub mod math {
    pub fn compute(angle: I32) -> I32 {
        angle
    }
}

pub fn run() -> I32 {
    math::compute(angle: 1.5)
}
