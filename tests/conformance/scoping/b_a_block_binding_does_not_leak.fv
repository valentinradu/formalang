// expect: reject UndefinedReference
// from: rust tests/ui/resolve/ (a binding is scoped to its block).
pub fn f() -> I32 {
    let a = {
        let inner: I32 = 1
        inner
    }
    a + inner
}
