// expect: run
// docs/user/structs.md: a later `let` of the same name hides the first.
// Its initializer still sees the first.
pub fn run_checks() {
    let x: I32 = 1
    let x: I32 = x + 1
    assert(condition: x == 2)
}
