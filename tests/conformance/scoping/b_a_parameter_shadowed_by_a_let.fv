// expect: run
// docs/user/structs.md: shadowing stays legal. A `let` may hide a
// parameter.
fn f(x: I32) -> I32 {
    let x: I32 = x * 10
    x
}

pub fn run_checks() {
    assert(condition: f(x: 2) == 20)
}
