// expect: reject OverlappingArguments
// docs/user/functions.md: two `mut` arguments must not reach one value.
fn swap(mut a: I32, mut b: I32) { }

pub fn f() {
    let mut t = (x: 1, y: 2)
    swap(a: t.x, b: t.x)
}
