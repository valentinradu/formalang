// expect: run
// The most specific overload wins: the one firing fewest defaults.

fn f(a: I32) -> I32 {
    1
}

fn f(a: I32, b: I32 = 0) -> I32 {
    2
}

pub fn run_checks() {
    assert(condition: f(a: 1) == 1)
    assert(condition: f(a: 1, b: 2) == 2)
}
