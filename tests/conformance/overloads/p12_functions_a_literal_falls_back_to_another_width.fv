// expect: run
// docs/user/functions.md: if no overload fits a literal exactly, the
// literal fits any numeric type of its kind.
fn only_wide(v: I64) -> I32 { 64 }
fn only_wide(v: String) -> I32 { 0 }

pub fn run_checks() {
    assert(condition: only_wide(v: 1) == 64)
}
