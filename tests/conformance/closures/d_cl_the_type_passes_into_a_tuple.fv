// expect: run
// docs/user/closures.md: the type goes through the elements of a tuple.
pub fn run_checks() {
    let t: (f: (I32) -> I32, n: I32) = (f: (x) -> x + 1, n: 1)
    assert(condition: t.f(t.n) == 2)
}
