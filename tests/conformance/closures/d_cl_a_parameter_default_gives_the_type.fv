// expect: run
// docs/user/closures.md: the declared type of a parameter default gives
// the type.
fn apply(n: I32, f: (I32) -> I32 = (x) -> x * 2) -> I32 { f(n) }

pub fn run_checks() {
    assert(condition: apply(n: 3) == 6)
}
