// expect: run
// docs/user/closures.md: a parameter default gives the closure its type.
fn apply(x: I32, f: (I32) -> I32 = (v) -> v + 100) -> I32 { f(x) }

pub fn run_checks() {
    assert(condition: apply(x: 1) == 101)
    assert(condition: apply(x: 1, f: (v) -> v) == 1)
}
