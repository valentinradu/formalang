// expect: run
// docs/user/closures.md: the type goes through the result of a block.
pub fn run_checks() {
    let f: (I32) -> I32 = {
        let k = 3
        (x) -> x * k
    }
    assert(condition: f(2) == 6)
}
