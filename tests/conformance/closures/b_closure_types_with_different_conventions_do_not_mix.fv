// expect: reject TypeMismatch
// docs/user/types.md: `(mut I32) -> I32` and `(I32) -> I32` are two types.
fn apply(f: (I32) -> I32) -> I32 { f(1) }

pub fn g() -> I32 {
    let h: (mut I32) -> I32 = (mut n) -> n
    apply(f: h)
}
