// expect: run
// docs/user/closures.md: a closure captures by value. A returned closure
// holds a copy of a local `let`, so it can capture one.
fn make() -> (I32) -> I32 {
    let k = 3
    (y) -> y + k
}

pub fn run_checks() {
    assert(condition: make()(1) == 4)
}
