// expect: run
// docs/user/closures.md: the type goes through the branches of `if`.
pub fn run_checks() {
    let up = true
    let f: (I32) -> I32 = if up { (x) -> x + 1 } else { (x) -> x - 1 }
    assert(condition: f(1) == 2)
}
