// expect: run
// docs/user/closures.md: an optional closure slot gives the type of its
// closure. This includes a `let` annotation.
pub fn run_checks() {
    let g: ((I32) -> I32)? = (x) -> x + 1
    if let h = g { assert(condition: h(1) == 2) } else { assert(condition: false) }
}
