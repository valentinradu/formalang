// expect: run
// docs/user/closures.md: the declared type of a field gives the type.
struct Op { f: (I32) -> I32 }

pub fn run_checks() {
    let op = Op(f: (x) -> x * 3)
    assert(condition: op.f(2) == 6)
}
