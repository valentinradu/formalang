// expect: run
pub fn add_base(base: I32, x: I32) -> I32 {
    let f = (n: I32) -> n + base
    f(x)
}

pub fn run_checks() {
    assert(condition: add_base(base: 10, x: 5) == 15)
    assert(condition: add_base(base: 0, x: 5) == 5)
}
