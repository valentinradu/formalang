// expect: reject TypeMismatch
// docs/user/closures.md: the parameter type gives the closure its type.
// The body must have the declared return type.
fn apply(f: (I32) -> Boolean) -> Boolean { f(1) }

pub fn g() -> Boolean {
    apply(f: (x) -> x + 1)
}
