// expect: run
// docs/user/closures.md: an optional closure slot gives the type of its
// closure.
pub fn run_checks() {
    let o: ((I32) -> I32)? = (x) -> x + 1
    let r = if let f = o { f(1) } else { 0 }
    assert(condition: r == 2)
}
