// expect: run
// docs/user/functions.md: defaults are positional from the right.
fn f(x: I32, y: I32 = 2, z: I32 = y * 10) -> I32 { x + y + z }

pub fn run_checks() {
    assert(condition: f(x: 1) == 23)
    assert(condition: f(x: 1, y: 3) == 34)
    assert(condition: f(x: 1, y: 3, z: 0) == 4)
}
