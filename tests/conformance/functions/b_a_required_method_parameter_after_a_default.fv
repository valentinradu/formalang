// expect: reject RequiredParamAfterDefault
// docs/user/functions.md: defaults must be positional from the right;
// `self` is not counted.
pub struct S { n: I32 }

impl S {
    fn f(self, x: I32 = 0, y: I32) -> I32 { x + y }
}
