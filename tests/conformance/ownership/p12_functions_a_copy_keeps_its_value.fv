// expect: run
// docs/user/functions.md: a copy in its own binding is a separate place.
fn put(mut a: I32, b: I32) { a = b }

pub fn run_checks() {
    let mut x: I32 = 1
    let y: I32 = x
    x = 7
    put(a: x, b: y)
    assert(condition: x == 1)
    assert(condition: y == 1)
}
