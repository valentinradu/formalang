// expect: reject UseAfterSink
// Two values of one literal sink one binding twice.
fn take(sink s: String) -> I32 { s.len() }

pub fn f() -> I32 {
    let s: String = "x"
    let d = ["a": take(s: s), "b": take(s: s)]
    d.len()
}
