// expect: reject UseAfterSink
// Two elements of one literal sink one binding twice.
fn take(sink s: String) -> I32 { s.len() }

pub fn f() -> I32 {
    let s: String = "x"
    let xs = [take(s: s), take(s: s)]
    xs.len()
}
